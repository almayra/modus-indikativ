import { Pagination } from "./Pagination";

export const FivePagePaginaton = () => {
  return <Pagination />;
};

const PaginationStory = {
  title: "Molecule/Pagination",
  component: Pagination,
};

export default PaginationStory;
